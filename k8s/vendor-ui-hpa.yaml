apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: vendor-ui-hpa
  namespace: msa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vendor-ui

  minReplicas: 1
  maxReplicas: 4

  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50

